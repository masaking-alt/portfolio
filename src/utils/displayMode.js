export function getModeLabel(displayMode) {
  if (displayMode === 'home') {
    return 'Masaking';
  }
  if (displayMode === 'cui') {
    return 'Masaking CUI';
  }
  if (displayMode === 'app') {
    return 'Masaking App';
  }
  return 'Masaking';
}
